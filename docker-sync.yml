version: "2"

syncs:
  docker-sync-mygov:
    src: './'
    sync_userid: '501'
    sync_excludes: ['.gitignore', '.git', '.idea', 'mariadb-init']
#  docker-sync-another-project:
#    src: '/absolute/path/to/project/codebase'
#    sync_userid: '501'
#    sync_excludes: ['.gitignore', '.git/', '.idea/']
